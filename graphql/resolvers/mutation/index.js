const graphql = require('graphql');const { GraphQLObjectType } = graphql;const UserMutation = require('./user');const IngredientMutation = require('./ingredient');const DishMutation = require('./dish');const CategoryMutation = require('./category');const ReservedMutation = require('./reserved');module.exports = new GraphQLObjectType({    name: 'Mutation',    fields: {        ...UserMutation,        ...IngredientMutation,        ...DishMutation,        ...CategoryMutation,        // not tested        ...ReservedMutation,    }});